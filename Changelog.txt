TightDB Changelog:
==================

Format:

2012-mm-dd
----------
! Fixed bug [github issuenr]: ....        (user visible bug fixed       - passed on to release notes)
+ Added feature ....                      (user visible new feature     - passed on to release notes)
- Removed/deprecated feature/method ....  (user visible removed feature - passed on to release notes)
. Any other notes ....                    (internal changes)

Changelog started 2013-10-03 Not so long before the first larger release of the binding

New changes are added to the top of the file

Things that are mentioned in this file :

- Public methods added or deleted
- Public methods renamed
- Public method signature (parametres) changed
- Public method behavior changed
- Public field or property in class added or deleted, or changed
- Changes to class names, or interfaces implemented

2013-10-07

+Table.GetSubTableSize(long,long) added
+Table.GetSubTableSize(String,long) added
+Table.GetMixedSubTableSize(long,long) added
+Table.GetMixedSubTableSize(String,long) added
+TableView-GetSubTableSize(long,long) added
+TableView-GetSubTableSize(string,long) added
+TableView.GetMixedSubTableSize(long,long) added
+TableView.GetMixedSubTableSize(string,long) added
. Added support for NET35 and NET40 in examples

2013-10-10
. Changed how the library is distributed
. Changed how projects use the library dll's
. Changed readme files to reflect new more simple way of deployment
. Updated all projects to reference dll's as content links
