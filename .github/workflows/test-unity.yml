name: test-unity
"on":
  workflow_call:
    inputs:
      version:
        type: string
        required: true
env:
  REALM_DISABLE_ANALYTICS: true
  DOTNET_NOLOGO: true
jobs:
  build-tests:
    runs-on:
    - unity
    - ${{ matrix.testData.os }}
    name: Build Unity ${{ matrix.testData.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        settings:
        - Mono-Net4
        testData:
        - os: windows
          platform: Windows64
    steps:
    - name: Fetch Unity Package
      uses: actions/download-artifact@v2
      with:
        name: io.realm.unity-${{ inputs.version }}.tgz
        path: Realm/Realm.Unity
    - name: Fetch Unity Tests
      uses: actions/download-artifact@v2
      with:
        name: UnityTests
        path: Tests/Tests.Unity
    - run: echo $PATH
      if: matrix.testData.os == 'linux'
    - name: Build Unity Tests
      run: |
        unity-editor -runTests -batchmode -projectPath ${{ github.workspace }}/Tests/Tests.Unity -testPlatform Standalone${{ matrix.testData.platform }} -testSettingsFile ${{ github.workspace }}/Tests/Tests.Unity/.TestConfigs/${{ matrix.settings }}.json -logFile ${{ matrix.testData.os == 'windows' && 'build.log' || '-'}}
        ${{ matrix.testData.os == 'windows' && 'cat build.log' || ''}}
    - name: Store artifacts for UnityTestsRunner.${{ matrix.testData.os }}
      uses: actions/upload-artifact@v2
      with:
        name: UnityTestsRunner.${{ matrix.testData.os }}
        path: ${{ github.workspace }}/Tests/Tests.Unity/Player_Standalone${{ matrix.testData.platform }}_${{ matrix.settings }}/
        retention-days: ${{ github.event_name != 'pull_request' && 30 || 1 }}
  run-tests-windows:
    runs-on: windows-latest
    name: Unity Windows
    timeout-minutes: 30
    needs:
    - build-tests
    steps:
    - name: Download Test Runner
      uses: actions/download-artifact@v2
      with:
        name: UnityTestsRunner.windows
        path: TestRunner
    - name: Run Tests
      run: |
        tree
        ${{ github.workspace }}\TestRunner\PlayerWithTests.exe -logFile test.log
        cat test.log
      shell: powershell
